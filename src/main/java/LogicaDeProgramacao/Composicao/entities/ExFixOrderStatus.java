package LogicaDeProgramacao.Composicao.entities;

public enum ExFixOrderStatus {

    /*  ENUM = É um tipo especial que serve para especificar de forma literal um conjunto de constantes relacionada
            é como se fosse uma cadeia de processo (aqui ele esta usando o exemplo do delivery)
     */

    PENDING_PAYMENT,
    PROCESSING,
    SHIPPED,
    DELIVERED;
}
